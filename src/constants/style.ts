import { Theme } from '@mui/material/styles';
import { SxProps } from '@mui/system';

export const INPUT_BASER_STYLE: SxProps<Theme> = { height: 40, '& .MuiInputBase-root': { height: 40 }, background: 'white' };
